<div class="{{classes.wrapper}}">
    <form class="{{classes.form}}">
        <h2 class="{{classes.headerText}}">{{headerText}}</h2>
        <div class="{{classes.fieldsWrapper}}">
            {{#each fields}}
                <label class="{{../classes.field}}" for="{{name}}">
                    <span class="{{../classes.title}}">{{title}}</span>
                    <input class="{{../classes.input}}" type="{{type}}" name="{{name}}" id="{{name}}">
                    <span class="{{../classes.error}}">{{errorText}}</span>
                </label>
            {{/each}}
        </div>

        <div class="{{classes.controls}}">
            <button class="{{classes.enterButton}}" type="submit">
                {{enterText}}
            </button>

            <a class={{classes.registerButton}} href="/register">{{registerText}}</a>
        </div>
    </form>
</div>
